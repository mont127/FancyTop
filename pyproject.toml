[project]
name = "personalproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cheesy porting", email = "84504416+mont127@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
personalproject = "personalproject:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
